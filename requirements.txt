# (Python 3.12, CUDA 12.9). Install PyTorch first (see README), then `pip install -e .`
#
# torch / torchvision / triton are installed separately from the PyTorch index
# so they are listed here as lower bounds only (do not let pip pull the CPU wheels):
#   torch==2.7.1  torchvision==0.22.1  triton==3.3.1

# --- core deep-learning stack ---
transformers==4.56.1
accelerate==1.10.1
diffusers==0.37.1
peft==0.17.1
safetensors==0.6.2
tokenizers==0.21.4
huggingface-hub==0.34.4
hf-xet==1.1.9
datasets==4.0.0
sentencepiece==0.2.1
bitsandbytes==0.46.1
einops==0.8.1

# --- numerics / data ---
numpy==2.4.4
pandas==2.3.2
scipy==1.16.2
scikit-learn==1.7.2

# --- vision / video I/O ---
pillow==11.2.1
opencv-python==4.13.0.92
opencv-python-headless==4.13.0.92
imageio==2.37.3
imageio-ffmpeg==0.6.0
av==17.0.1
decord==0.6.0
ffmpeg-python==0.2.0
mediapy==1.1.6
matplotlib==3.10.6

# --- reward / eval clients (OpenAI-compatible vLLM endpoints) ---
openai>=1.0
qwen-vl-utils>=0.0.14

# --- config / utils ---
omegaconf==2.3.0
ml_collections==1.1.0
easydict==1.13
absl-py==2.3.0
ftfy==6.3.1
regex==2025.9.1
protobuf==4.24.4
pydantic==2.11.7
requests==2.32.4
aiohttp==3.12.15
tqdm==4.67.1
wandb==0.21.4
nvidia-ml-py==13.580.82

# --- reward servers (vLLM) ---
fastapi==0.116.1
uvicorn==0.35.0

# --- optional extras (uncomment as needed) ---
# flash-attn is prebuilt in the reference environment (flash_attn==2.8.1); install
# a wheel matching your CUDA/torch if you need FlashAttention.
# flash-attn==2.8.1
#
# OCR reward (flow_grpo/ocr.py):
# paddleocr==2.9.1
# paddlepaddle-gpu==2.6.2
# python-Levenshtein==0.27.1
#
# Legacy image reward models (flow_grpo/hpsv2_scorer.py, imagereward_scorer.py, gen_eval.py):
# hpsv2==1.2.0
# image-reward==1.5
# open_clip_torch==3.1.0
# clip @ git+https://github.com/openai/CLIP.git
#
# deepspeed==0.16.4              # flow_grpo/videoalign/utils.py
# xfuser                         # multi-GPU sequence-parallel Wan inference
# dashscope                      # optional DashScope prompt-extend backend
